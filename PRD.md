# PRD - משחק זיכרון (Card Memory Game)

## 1. מטרה ובעיה

הפרויקט נבנה כפרויקט מסכם לקורס באוניברסיטה. המטרה היא לבנות משחק זיכרון (Memory / Concentration) מבוסס דפדפן, שבו השחקן חושף כרטיסים בזוגות ומנסה למצוא את כל הזוגות התואמים במספר המהלכים הקטן ביותר ובזמן הקצר ביותר.

הבעיה שהפרויקט פותר היא בעיקר אקדמית/הדגמתית: להוכיח שליטה בבניית אפליקציית React מודרנית עם ניהול מצב (state management), לוגיקת משחק, טיימר, ושמירת נתונים מקומית - תוך שמירה על קוד נקי, מודולרי וניתן לתחזוקה.

## 2. משתמש יעד / תרחיש שימוש

- **משתמש יעד:** משתמש בודד (יחיד) הגולש לאתר מהדפדפן (מחשב או מובייל), ללא צורך בהרשמה או התחברות.
- **תרחיש שימוש טיפוסי:**
  1. המשתמש נכנס לאתר.
  2. בוחר רמת קושי (קל / בינוני / קשה).
  3. לוח כרטיסים נטען כשכל הכרטיסים הפוכים.
  4. המשתמש לוחץ על כרטיס אחר כרטיס בניסיון לגלות זוגות תואמים.
  5. הטיימר רץ מרגע תחילת המשחק ועד שכל הזוגות נמצאו.
  6. בסיום המשחק מוצג סיכום (זמן, מספר מהלכים, ניצחון).
  7. הסטטיסטיקה נשמרת מקומית ומוצגת למשתמש גם בביקורים חוזרים.

## 3. היקף (Scope)

### בתוך ההיקף (In Scope)
- אפליקציית Frontend בלבד (Single Page Application).
- לוח משחק בגודל n×n בהתאם לרמת הקושי שנבחרה.
- לוגיקת חשיפת כרטיסים, בדיקת התאמה, ונעילת זוגות שנמצאו.
- טיימר משחק (התחלה, עצירה בסיום).
- 3 רמות קושי (למשל: קל 4×4, בינוני 6×6, קשה 8×8 - להחלטה סופית בשלב התכנון הטכני).
- שמירת סטטיסטיקות משחק (זמן, ניצחונות/הפסדים, מספר מהלכים) באמצעות LocalStorage.
- מסך/אזור להצגת היסטוריית סטטיסטיקות למשתמש.
- עיצוב בסיסי ונקי (CSS), רספונסיבי ברמה סבירה.
- **(בונוס)** בחירת סוג חפיסה: חפיסה רגילה (סמלים/אימוג'ים קבועים) או חפיסת "דגלי מדינות" הנמשכת בזמן אמת מ-API ציבורי חינמי (Read-only), כולל טיפול במצבי טעינה ושגיאה.
- **(בונוס)** אנימציית ניצחון (Confetti) המופעלת בסיום משחק מוצלח, לצד הודעת הניצחון הקיימת.

### מחוץ להיקף (Out of Scope)
- אין פיתוח צד שרת (Backend) עצמי - אין API בבעלותנו ואין בסיס נתונים מרוחק. הצריכה של API ציבורי קיים וחיצוני (עבור חפיסת דגלי המדינות) היא קריאה חד-כיוונית (Read-only) מהדפדפן בלבד, ואינה נחשבת פיתוח שרת.
- אין מערכת משתמשים מרובים, אין הרשמה/התחברות, אין אימות (Authentication).
- אין ריבוי שחקנים (Multiplayer) או משחק מקוון בזמן אמת.
- אין שיתוף תוצאות ברשתות חברתיות.
- אין תמיכה Offline מלאה (PWA) - לא נדרש בשלב זה.
- אין תמיכה בריבוי שפות (i18n) מעבר לעברית/אנגלית בסיסית.
- אין שימוש ב-API בתשלום או כזה הדורש מפתח (API Key) סודי - רק שירותים ציבוריים וחינמיים.

## 4. דרישות פונקציונליות

1. **לוח כרטיסים (n×n):**
   - יצירת לוח דינמי בגודל המתאים לרמת הקושי שנבחרה.
   - הכרטיסים מכילים זוגות תוכן (תמונות/אימוג'ים/סמלים) המעורבבים אקראית בכל משחק חדש.

2. **חשיפת כרטיסים והתאמת זוגות:**
   - לחיצה על כרטיס הופכת אותו וחושפת את תוכנו.
   - ניתן לחשוף עד שני כרטיסים בו-זמנית לצורך השוואה.
   - אם שני הכרטיסים החשופים תואמים - הם נשארים גלויים ו"ננעלים" כמותאמים.
   - אם אינם תואמים - הם הופכים בחזרה אחרי השהיה קצרה.
   - מניעת לחיצה על כרטיס שכבר נחשף/הותאם, ומניעת לחיצה על כרטיס שלישי בזמן שממתינים להשוואה.

3. **טיימר:**
   - הטיימר מתחיל עם תחילת המשחק (או עם הלחיצה הראשונה על כרטיס).
   - מוצג בזמן אמת למשתמש (שניות/דקות).
   - נעצר אוטומטית כאשר כל הזוגות נמצאו (סיום משחק).

4. **רמות קושי:**
   - 3 רמות קושי הניתנות לבחירה לפני תחילת המשחק.
   - כל רמה קובעת גודל לוח שונה (ולכן מספר זוגות שונה) ורמת אתגר שונה.

5. **שמירת סטטיסטיקות משחק (LocalStorage):**
   - בסיום כל משחק נשמרים: זמן המשחק, מספר מהלכים, רמת הקושי, תוצאה (ניצחון), ותאריך.
   - הנתונים נשמרים ב-LocalStorage של הדפדפן ונטענים מחדש בכל כניסה לאפליקציה.
   - הצגת סיכום/היסטוריית סטטיסטיקות למשתמש (למשל: שיא זמן לכל רמת קושי, מספר משחקים ששוחקו).
   - אפשרות לאפס את הסטטיסטיקות השמורות.

6. **בחירת סוג חפיסה ומשיכת נתונים מ-API (בונוס):**
   - לצד בחירת רמת הקושי, השחקן יכול לבחור את סוג חפיסת הקלפים: "חפיסה רגילה" (הסמלים/האימוג'ים הקבועים) או "חפיסת דגלי מדינות".
   - כאשר נבחרה חפיסת דגלי מדינות, האפליקציה תמשוך בזמן אמת רשימת קודי מדינות ממקור API ציבורי, חינמי ופתוח (ללא מפתח) - כגון flagcdn.com - ותבחר ממנה באופן אקראי מספר מדינות התואם למספר הזוגות הנדרש ברמת הקושי שנבחרה. סמל הדגל (אימוג'י) מחושב מקוד המדינה (ISO 3166-1 alpha-2) בצד הלקוח, כדי להישאר עקבי ויזואלית עם חפיסת הסמלים הרגילה. (לתשומת לב: REST Countries, שנשקל בתחילה, הפך לשירות בתשלום/עם מפתח וננטש לטובת חלופה חינמית באמת.)
   - בזמן משיכת הנתונים מוצג למשתמש מצב טעינה (Loading) ברור.
   - במקרה של כשל בקריאת ה-API (לדוגמה: אין אינטרנט, השירות אינו זמין) תוצג הודעת שגיאה ידידותית, והמשחק יחזור אוטומטית (Fallback) לחפיסה הרגילה כך שהשחקן תמיד יוכל להתחיל משחק.
   - קריאת ה-API היא read-only בלבד ואינה דורשת מפתח API בתשלום, בהתאם להיקף הפרויקט (ללא צד שרת עצמי).

7. **אנימציית ניצחון (בונוס):**
   - בסיום משחק מוצלח (כאשר כל הזוגות נמצאו) תופעל אנימציית קונפטי (Confetti) על גבי המסך, בנוסף להודעת הניצחון הקיימת.
   - האנימציה מופעלת פעם אחת לכל ניצחון, ונעצרת/מתאפסת אוטומטית בעת התחלת משחק חדש או מעבר לרמת קושי אחרת.

## 5. בחירות טכניות

- **React** - ניהול קומפוננטות ו-state של הלוח, הכרטיסים, הטיימר והסטטיסטיקות.
- **Vite** - כלי build ופיתוח מהיר (dev server, HMR, bundling).
- **CSS** - עיצוב באמצעות CSS רגיל (או CSS Modules), ללא תלות בספריות UI חיצוניות כבדות.
- **LocalStorage (Web API)** - שמירת נתונים מקומית בדפדפן, ללא צד שרת.
- **Fetch API (מובנה בדפדפן)** - משיכת רשימת קודי מדינות מ-API ציבורי חיצוני, חינמי וללא מפתח (flagcdn.com) עבור חפיסת הבונוס.
- **ספריית קונפטי** (למשל `react-confetti` או `canvas-confetti`) - להפעלת אנימציית הניצחון.
- קוד ב-JavaScript (או JSX סטנדרטי של React), מבנה קבצים מודולרי לפי קומפוננטות.

## 6. קריטריון הצלחה (Definition of Done)

הפרויקט ייחשב מוכן (Done) כאשר:

- ניתן להריץ את האפליקציה מקומית עם `npm run dev` ולשחק משחק זיכרון מלא מתחילתו ועד סופו ללא שגיאות.
- ניתן לבחור בין 3 רמות קושי שונות, וכל רמה מציגה לוח בגודל תואם.
- לוגיקת ההתאמה בין כרטיסים פועלת נכון בכל המקרים (כולל מניעת קליקים לא חוקיים).
- הטיימר מציג זמן מדויק ומפסיק לרוץ עם סיום המשחק.
- בסיום משחק מוצג סיכום תוצאות, והנתונים נשמרים ב-LocalStorage ונטענים מחדש בטעינה חוזרת של הדף.
- הממשק ברור, נקי, וניתן לשימוש הן במחשב והן במסך מובייל (רספונסיבי ברמה בסיסית).
- הקוד מאורגן בקומפוננטות נפרדות וברורות, ללא קוד מת או קונסולים מיותרים.
- אין שגיאות ב-console וב-lint (ESLint) בעת הרצת הפרויקט.
- ניתן לבחור בין חפיסה רגילה לחפיסת דגלי מדינות; כאשר נבחרת חפיסת הדגלים, הנתונים נמשכים בהצלחה מה-API ומוצגים כראוי, כולל טיפול תקין במצבי טעינה ובמצבי שגיאה (עם Fallback לחפיסה הרגילה).
- בסיום משחק מוצלח מופעלת אנימציית קונפטי לצד הודעת הניצחון.

# תוכנית עבודה - משחק זיכרון (Card Memory Game)

תוכנית זו מפרקת את [PRD.md](./PRD.md) למשימות קונקרטיות וקטנות, כל אחת מתאימה ל-commit בודד ומשמעותי.

## שלב 1: ניקוי והכנת בסיס הפרויקט (Vite Boilerplate)

- [x] הסרת קבצי הדוגמה של Vite/React הראשוניים שאינם נחוצים (למשל תוכן ברירת המחדל של `App.jsx`, לוגואים לא רלוונטיים ב-`src/assets`)
- [x] ניקוי `App.css` ו-`index.css` מסגנונות ברירת המחדל, והגדרת סגנון בסיס נקי (reset, פונט, צבעי רקע כלליים)
- [x] הגדרת מבנה תיקיות ראשוני בתוך `src` (למשל: `components/`, `hooks/`, `utils/`)
- [x] עדכון `README.md` עם תיאור קצר של הפרויקט ואופן ההרצה

## שלב 2: קומפוננטות UI פשוטות (ללא לוגיקה)

- [x] יצירת קומפוננטת `Card` סטטית (מציגה צד גלוי/צד מוסתר של כרטיס בודד, ללא לוגיקת קליקים)
- [x] יצירת קומפוננטת `Board` שמקבלת מערך כרטיסים ומרנדרת אותם בגריד n×n סטטי
- [x] יצירת קומפוננטת `Header`/`GameInfo` בסיסית (כותרת המשחק, מקום ריק לטיימר ומונה מהלכים)
- [x] הוספת עיצוב CSS בסיסי לגריד ולכרטיסים (גודל, ריווח, מצב הפוך/גלוי)

## שלב 3: לוגיקת משחק טריוויאלית (משחק יחיד, קבוע מראש)

- [x] יצירת פונקציית עזר ליצירת סט כרטיסים (זוגות) עם ערכים קבועים לצורך פיתוח ראשוני
- [x] חיבור `Board` ל-state של React כך שקליק על כרטיס הופך אותו (חשיפה/הסתרה בסיסית)
- [x] מימוש לוגיקת השוואת שני כרטיסים חשופים וסימונם כ"תואמים" כאשר מתאימים
- [x] מימוש הסתרה אוטומטית (עם השהיה) של שני כרטיסים לא-תואמים
- [x] מניעת קליקים לא חוקיים (קליק על כרטיס שכבר נעול, או קליק שלישי בזמן השוואה)
- [x] הוספת מונה מהלכים (Moves Counter) ועדכונו בכל זוג נבדק

## שלב 4: ערבוב אקראי ותנאי סיום משחק

- [x] מימוש פונקציית ערבוב (shuffle) של הכרטיסים בתחילת כל משחק
- [x] מימוש זיהוי סיום משחק (כל הזוגות נמצאו) והצגת הודעת ניצחון בסיסית
- [x] הוספת כפתור "התחל משחק חדש" שמאתחל את מצב הלוח והמהלכים

## שלב 5: טיימר

- [x] יצירת hook מותאם אישית `useTimer` (התחלה, עצירה, איפוס)
- [x] חיבור הטיימר להתחלה עם הקליק הראשון על כרטיס (או עם תחילת המשחק)
- [x] עצירת הטיימר אוטומטית עם סיום המשחק
- [x] הצגת הזמן הרץ בקומפוננטת ה-`GameInfo` בפורמט קריא (דקות:שניות)

## שלב 6: רמות קושי

- [x] יצירת קומפוננטת בחירת רמת קושי (קל / בינוני / קשה) המוצגת לפני תחילת המשחק
- [x] הגדרת מיפוי בין רמת קושי לגודל לוח ומספר זוגות (למשל קל=4×4, בינוני=6×6, קשה=8×8)
- [x] חיבור הבחירה ליצירת הלוח כך שגודל הלוח וסט הכרטיסים משתנים בהתאם לרמה שנבחרה
- [x] הוספת אפשרות לחזור למסך בחירת רמת קושי מתוך מסך המשחק (למשל כפתור "רמת קושי חדשה")

## שלב 7: סטטיסטיקות ו-LocalStorage

- [x] יצירת מודול `utils/storage.js` עם פונקציות קריאה/כתיבה גנריות ל-LocalStorage
- [x] מימוש שמירת תוצאת משחק בודד (זמן, מהלכים, רמת קושי, תאריך) בסיום כל משחק
- [x] יצירת קומפוננטת `Stats` המציגה היסטוריית משחקים/שיאים מתוך הנתונים השמורים
- [x] הוספת אפשרות איפוס (ניקוי) הסטטיסטיקות השמורות מתוך ממשק המשתמש

## שלב 8: ליטוש וגימור

- [x] שיפור רספונסיביות העיצוב למסכי מובייל (בדיקת גדלי לוח שונים)
- [x] מעבר על כל הקומפוננטות והסרת קוד מת, console.log, והערות מיותרות
- [x] הרצת ESLint ותיקון כל האזהרות/שגיאות
- [x] בדיקה ידנית מקצה לקצה: משחק מלא בכל שלוש רמות הקושי, כולל בדיקת שמירת סטטיסטיקות אחרי רענון דף
